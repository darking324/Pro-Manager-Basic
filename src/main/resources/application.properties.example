# Copy this file to application.properties and set values for your local environment.
# Environment variables override these values:
# PROMANAGE_DB_URL, PROMANAGE_DB_USER, PROMANAGE_DB_PASSWORD

db.url=jdbc:postgresql://localhost:5432/promanage
db.user=postgres
db.password=change-me
